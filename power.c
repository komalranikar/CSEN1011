#include <stdio.h>
#include <math.h>
int main(){
	int i,j;
	printf("enter your base = ");
	scanf("%lf",&i);
	printf("enter your exponent = ");
	scanf("%lf",&j);
	printf("your calculated power value is-- %f",pow(i,j));
}
